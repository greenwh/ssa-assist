# SSA Form-Assist Environment Variables
# Copy this file to .env or .env.local and add your API keys

# IMPORTANT:
# - Environment variables will pre-populate the Settings page
# - Users can override these values in the Settings UI
# - Overridden values are encrypted and stored in IndexedDB
# - This is useful for mobile users who cannot access .env files

# Anthropic Claude API
VITE_ANTHROPIC_API_KEY="sk-ant-..."
VITE_ANTHROPIC_API_MODEL=claude-sonnet-4-5-20250929

# Google Gemini API
VITE_GOOGLE_API_KEY="..."
VITE_GEMINI_API_MODEL=gemini-2.0-flash-exp

# OpenAI API
VITE_OPENAI_API_KEY="sk-proj..."
VITE_OPENAI_API_MODEL=gpt-4o-mini

# xAI API
VITE_XAI_API_KEY="xai-..."
VITE_XAI_API_MODEL=grok-beta

# Note: All environment variables must be prefixed with VITE_ to be exposed to the client
# Never commit your .env or .env.local file to git!
